1. Lines 22-28 are floating at the top level of the module — they'll
  try to run once when the file loads, but pglite doesn't exist yet. Plus
   line 23-24 has a broken SQL string (newline in the middle).
  2. The actual beforeEach on lines 33-36 still uses the plaintext
  password hash.

  Fix:
  1. Delete lines 22-28 entirely
  2. Replace lines 33-36 (the original INSERT inside beforeEach) to hash
  first, then use the hash:

  const hash = await bcrypt.hash("hashed_password_123", 10);
  await pglite.query("INSERT INTO users (username, password_hash) VALUES
  ($1, $2)", [
    "testuser",
    hash,
  ]);

  The hashing needs to happen inside beforeEach because it runs before
  each test and needs the fresh pglite instance.